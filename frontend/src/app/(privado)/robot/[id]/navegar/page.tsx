/**
 * La pantalla de navegación.
 *
 * ⚠️ Hoy no puede funcionar y lo dice ella misma: `atriz-nav.service` está
 *    instalado y **sin habilitar**, y no existe un mapa del aula. La pantalla
 *    existe igualmente porque un hueco declarado se puede planificar y uno
 *    callado se lee como «esto ya está resuelto».
 */
import { PanelNavegar } from '@/componentes/robot/PanelNavegar'

export default function PaginaNavegar() {
  return <PanelNavegar />
}
